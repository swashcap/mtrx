import { styled } from '@linaria/react';

export const ModalMask = styled.div`

  background: var(--color-background);
  bottom: 0;
  left: 0;
  opacity: 0.75;
  overflow: hidden;
  position: absolute;
  right: 0;
  top: 0;
`;
